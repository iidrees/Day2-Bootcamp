'use strict'

function reverseString(strs) {
	if (!str || strs.length === 0) {
		return null
	} else if(strings.length > 1){
	return strings.reverse().join('')
    } else if (string === string.reverse().join('')) {
    	return true
    };
};

module.exports = reverseString(strs)